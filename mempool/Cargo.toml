[package]
name = "axiom-mempool"
version = "0.1.0"
edition = "2021"

[dependencies]
axiom-primitives = { path = "../primitives" }
axiom-crypto = { path = "../crypto" }
thiserror = "1.0"
serde = { version = "1.0", features = ["derive"] }
log = "0.4"
