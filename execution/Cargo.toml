[package]
name = "axiom-execution"
version = "0.1.0"
edition = "2021"

[dependencies]
axiom-primitives = { path = "../primitives" }
axiom-crypto = { path = "../crypto" }
axiom-state = { path = "../state" }
thiserror = "1.0"

[dev-dependencies]
rand = "0.8"
